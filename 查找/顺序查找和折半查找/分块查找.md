# 算法实现
```c
#include <stdio.h>
#include <stdlib.h>
#define Maxsize 100

typedef int ElemType;
typedef struct
{
    ElemType max_value;
    int low_index;
    int high_index;
} IndexTable;

int main()
{
    ElemType SequenceList[Maxsize];
    IndexTable IT[10]; // 将顺序表分成10个块，并用索引表指明
}

```